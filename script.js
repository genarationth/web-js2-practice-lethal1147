// Challenge 1: Create an Age Calculator

const birthYear = 2000;
const futureYear = 2028;
let age = futureYear - birthYear;
console.log(`If you were born in ${birthYear}, then in ${futureYear} you’ll be ${age - 1} or ${age} (depending on the month in ${futureYear}).`)

// Challenge 2: Create an information card

const person = {
    name: 'Kim',
    location: 'Bangkok',
    hobby: 'Play games',
}

console.log(`Hi, my name is ${person.name}. I live in ${person.location} and enjoy ${person.hobby}.`)